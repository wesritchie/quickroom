name: Refresh QuickRoom Dashboards

on:
  schedule:
    # All times in UTC. ET = UTC - 4 (EDT) or UTC - 5 (EST)
    # Geo Totem: 6:00 AM UTC = 2:00 AM ET
    - cron: '0 6 * * *'
    # Brick House revenue history: 7:00 AM UTC on the 1st of every month
    - cron: '0 7 1 * *'
  workflow_dispatch:
    inputs:
      dashboard:
        description: 'Which dashboard to refresh'
        required: true
        type: choice
        options:
          - all
          - geo-totem
          - brick-house
          - brick-house-history
          - rainbow-review

permissions:
  contents: write

jobs:
  geo-totem:
    if: >
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && (inputs.dashboard == 'all' || inputs.dashboard == 'geo-totem'))
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Refresh Geo Totem data
        env:
          VERISCAN_TOKEN: ${{ secrets.VERISCAN_TOKEN }}
        run: python scripts/refresh_geo_totem.py

      - name: Commit and push
        run: |
          git config user.name "QuickRoom Pipeline"
          git config user.email "pipeline@quickroom.local"
          git add geo-totem.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Daily Geo Totem refresh — $(date +%Y-%m-%d)"
            git push
          fi

  brick-house:
    if: >
      always() &&
      (github.event_name == 'schedule' ||
       (github.event_name == 'workflow_dispatch' && (inputs.dashboard == 'all' || inputs.dashboard == 'brick-house'))) &&
      (needs.geo-totem.result == 'success' || needs.geo-totem.result == 'skipped')
    runs-on: ubuntu-latest
    needs: geo-totem
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          # Pull latest in case geo-totem just committed
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Refresh Brick House data
        env:
          DUTCHIE_API_KEY_DRACUT: ${{ secrets.DUTCHIE_API_KEY_DRACUT }}
          DUTCHIE_API_KEY_PEPPERELL: ${{ secrets.DUTCHIE_API_KEY_PEPPERELL }}
          DUTCHIE_API_KEY_GROTON: ${{ secrets.DUTCHIE_API_KEY_GROTON }}
          LIT_ALERTS_TOKEN: ${{ secrets.LIT_ALERTS_TOKEN }}
        run: python scripts/refresh_brick_house.py

      - name: Commit and push
        run: |
          git config user.name "QuickRoom Pipeline"
          git config user.email "pipeline@quickroom.local"
          git add brick-house.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Daily Brick House refresh — $(date +%Y-%m-%d)"
            git pull --rebase origin main
            git push
          fi

  rainbow-review:
    # Daily Rainbow Review data refresh. Replaces the old Cowork / Chrome
    # pipeline that relied on the window.name bridge + CM6 dispatch —
    # that pipeline was timing out mid-run every day starting 2026-04-18.
    #
    # Calls the existing Apps Script proxy for both Dutchie + Lit Alerts
    # data (the proxy already holds both API keys server-side, so this job
    # needs no secrets), writes rainbow-review-data.js, and commits.
    if: >
      always() &&
      (github.event_name == 'schedule' ||
       (github.event_name == 'workflow_dispatch' && (inputs.dashboard == 'all' || inputs.dashboard == 'rainbow-review'))) &&
      (needs.brick-house.result == 'success' || needs.brick-house.result == 'skipped' || needs.brick-house.result == 'failure')
    runs-on: ubuntu-latest
    needs: brick-house
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Refresh Rainbow Review data
        run: python scripts/refresh_rainbow_review.py

      - name: Commit and push
        run: |
          git config user.name "QuickRoom Pipeline"
          git config user.email "pipeline@quickroom.local"
          git add rainbow-review-data.js
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Daily Rainbow Review refresh — $(date +%Y-%m-%d)"
            git pull --rebase origin main
            git push
          fi

  brick-house-history:
    # Monthly revenue history refresh. Triggers:
    #   - The '1st-of-month' schedule cron ('0 7 1 * *')
    #   - Manual dispatch with dashboard=brick-house-history (or =all)
    # Runs in top-up mode (re-pulls previous + current month and merges
    # into existing storeData). Idempotent — safe to rerun.
    if: >
      always() &&
      ((github.event_name == 'schedule' && github.event.schedule == '0 7 1 * *') ||
       (github.event_name == 'workflow_dispatch' && (inputs.dashboard == 'all' || inputs.dashboard == 'brick-house-history'))) &&
      (needs.rainbow-review.result == 'success' || needs.rainbow-review.result == 'skipped' || needs.rainbow-review.result == 'failure')
    runs-on: ubuntu-latest
    needs: rainbow-review
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Refresh Brick House revenue history
        env:
          DUTCHIE_API_KEY_DRACUT: ${{ secrets.DUTCHIE_API_KEY_DRACUT }}
          DUTCHIE_API_KEY_PEPPERELL: ${{ secrets.DUTCHIE_API_KEY_PEPPERELL }}
          DUTCHIE_API_KEY_GROTON: ${{ secrets.DUTCHIE_API_KEY_GROTON }}
        run: python scripts/refresh_brick_house_history.py

      - name: Commit and push
        run: |
          git config user.name "QuickRoom Pipeline"
          git config user.email "pipeline@quickroom.local"
          git add brick-house.html
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Monthly Brick House history refresh — $(date +%Y-%m-%d)"
            git pull --rebase origin main
            git push
          fi

  notify:
    if: always()
    needs: [geo-totem, brick-house, rainbow-review, brick-house-history]
    runs-on: ubuntu-latest
    steps:
      - name: Report status
        run: |
          echo "=== QuickRoom Pipeline Status ==="
          echo "Geo Totem:           ${{ needs.geo-totem.result }}"
          echo "Brick House:         ${{ needs.brick-house.result }}"
          echo "Rainbow Review:      ${{ needs.rainbow-review.result }}"
          echo "Brick House History: ${{ needs.brick-house-history.result }}"
          if [[ "${{ needs.geo-totem.result }}" == "failure" ]] || \
             [[ "${{ needs.brick-house.result }}" == "failure" ]] || \
             [[ "${{ needs.rainbow-review.result }}" == "failure" ]] || \
             [[ "${{ needs.brick-house-history.result }}" == "failure" ]]; then
            echo "::error::One or more dashboard refreshes failed!"
            exit 1
          fi
